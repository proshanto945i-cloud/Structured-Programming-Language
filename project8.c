#include <stdio.h>
int main() 
{
    int a , b ,c ;

    a=b=c=500; // multiple variables declared and initialized in a single line

    printf("%d", a+b+c); // this will print the sum of a, b and c
    return 0;
}
